// import express from "express";
// import cors from "cors";
// import 'dotenv/config';
// import aiAdviceRoute from "./api/ai-advice.js";

// const app = express();

// // ✅ Middleware
// app.use(cors());          // permite peticiones desde cualquier origen (puedes restringir más tarde)
// app.use(express.json());   // parsea JSON automáticamente

// app.use("/api", aiAdviceRoute);

// app.listen(3001, () =>
//   console.log("API IA corriendo en http://localhost:3001")
// );
